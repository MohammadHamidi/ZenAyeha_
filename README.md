# ZenAyeha

یک پلتفرم گفتگوی هوشمند با رابط کاربری مینیمال و حرفه‌ای

## ویژگی‌ها

- ✨ رابط کاربری مینیمال و حرفه‌ای
- 🎯 سیستم ویزارد برای شخصی‌سازی تجربه کاربر
- 💬 رابط گفتگوی پیشرفته با پشتیبانی از Markdown
- 🐳 آماده برای استقرار با Docker
- ☁️ سازگار با Coolify

## ساختار پروژه

```
ZenAyeha/
├── index.html          # صفحه اصلی
├── styles.css          # استایل‌های مینیمال
├── wizard.js           # منطق ویزارد و چت
├── Icon.png           # آیکون برند
├── server.js          # سرور Express
├── package.json       # وابستگی‌های Node.js
├── Dockerfile         # فایل Docker
└── docker-compose.yml # تنظیمات Docker Compose
```

## نصب و راه‌اندازی محلی

### پیش‌نیازها

- Node.js 18 یا بالاتر
- npm یا yarn

### نصب

```bash
# نصب وابستگی‌ها
npm install

# راه‌اندازی در حالت توسعه
npm run dev

# راه‌اندازی در حالت تولید
npm start
```

سپس به آدرس `http://localhost:3000` مراجعه کنید.

## استقرار با Docker

### ساخت و اجرای کانتینر

```bash
# ساخت تصویر Docker
docker build -t zenayeha .

# اجرای کانتینر
docker run -d -p 3000:3000 --name zenayeha zenayeha
```

### استفاده از Docker Compose

```bash
# راه‌اندازی با Docker Compose
docker-compose up -d

# مشاهده لاگ‌ها
docker-compose logs -f

# توقف سرویس
docker-compose down
```

## استقرار در Coolify

### روش 1: استفاده از Git Repository

1. پروژه خود را در یک Git repository قرار دهید
2. در Coolify، یک پروژه جدید ایجاد کنید
3. منبع را به Git repository متصل کنید
4. Coolify به طور خودکار `Dockerfile` را تشخیص می‌دهد
5. پورت را روی 3000 تنظیم کنید
6. دکمه Deploy را بزنید

### روش 2: استفاده از Docker Compose

1. در Coolify، یک پروژه Docker Compose ایجاد کنید
2. محتوای `docker-compose.yml` را در تنظیمات وارد کنید
3. متغیرهای محیطی را تنظیم کنید (در صورت نیاز)
4. پروژه را deploy کنید

### تنظیمات محیطی

می‌توانید متغیرهای محیطی زیر را در Coolify تنظیم کنید:

- `PORT`: پورت سرور (پیش‌فرض: 3000)
- `NODE_ENV`: محیط اجرا (production/development)

### Health Check

سرویس شامل health check endpoint است که در آدرس `/health` قابل دسترسی است. Coolify از این endpoint برای بررسی وضعیت سرویس استفاده می‌کند.

## ساختار API

### Health Check

```
GET /health
```

پاسخ:
```json
{
  "status": "ok",
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

## توسعه

### اجرا در حالت توسعه

```bash
npm run dev
```

این دستور از `nodemon` برای رفرش خودکار استفاده می‌کند.

## مجوز

MIT

